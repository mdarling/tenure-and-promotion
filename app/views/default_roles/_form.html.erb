<%= form_for(@default_role) do |f| %>
  <% if @default_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@default_role.errors.count, "error") %> prohibited this default_role from being saved:</h2>

      <ul>
      <% @default_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
