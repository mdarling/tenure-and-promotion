<h1>Listing Added Roles</h1>
<h3>This user is able to create other users that have these roles:</h3>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Role</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @owned_roles.each do |owned_role| %>
      <tr>
        <td><%= owned_role.role %></td>
        <td><%= link_to 'Remove', user_owned_role_path(@user,owned_role), method: :delete, data: { confirm: 'Are you sure?' }, class: "badge" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add extra role', new_user_owned_role_path, class: "btn btn-primary" %>
